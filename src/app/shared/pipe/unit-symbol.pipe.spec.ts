import { UnitSymbolPipe } from './unit-symbol.pipe';

describe('UnitSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
